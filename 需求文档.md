# 前端宝典 (Frontend Bible) 需求与架构文档

## 1. 项目概述
本项目旨在打造一个“驾考宝典”风格的前端知识刷题与学习系统。打破传统的长篇大论学习模式，通过“碎片化知识点 + 针对性刷题 + 错题反复攻克 + 原文检索溯源”的闭环，帮助用户高效备战前端面试并巩固核心技术栈。

## 2. 核心技术栈
- **前端**：Vue 3 (Composition API) + Vue Router + Pinia + Tailwind CSS + Vite
- **后端**：Node.js + Express
- **数据库**：MySQL (mysql2 驱动)
- **文档解析**：Marked.js (前端渲染 Markdown)

## 3. 功能模块结构
系统采用移动端 App 风格的底部导航栏（Tab Bar）设计，分为四个核心入口与一个文档入口：

1. **章节练习 (Select Chapter & Knowledge)**：
   - 按大章节（HTML5, CSS3, JS, ES6, Vue3）平铺展示。
   - 点击章节后展示细分的具体知识点。
   - 用户可以选择某个知识点进行专向的客观题（单选）刷题训练。

2. **参阅文档 (Docs)**：
   - 将硬盘中沉重的 `.md` 原文进行切片后存入数据库 `knowledge_docs` 表。
   - **全文搜索**：支持在输入框检索知识点标题或正文，瞬间过滤并展示相关的原版文档切片。
   - **纯净阅读**：剔除冗余图片，保留最核心的代码块和文字解析。

3. **错题本 (Mistakes)**：
   - 用户在练习或考试中答错的题目会自动收录至此，并累计错误次数（`error_count`）。
   - 用户必须在错题本中重新答对，并手动点击“我已彻底掌握，移除该错题”方可将其移出，强化记忆。

4. **模拟考试 (Mock Exam)**：
   - 在限定时间内（或无限制时间下）从题库中随机抽取规定数量的题目进行全真模拟测试，完成后给出得分与解析报告。

## 4. 数据库设计要点
当前已建立以下核心表结构：
- `categories`：存储五大核心章节。
- `knowledge_docs`：存储从 `.md` 切割出的真实知识点切片。
- `questions`：存储选择题题库，通过 `category_id` 和 `knowledge_point` 字段与文档强关联。
- `mistakes`：存储用户的错题记录，记录题目的 ID 与答错次数。

*(注：后续系统重构时，将严格遵守用户制定的全局数据库规范：表名使用 `app_` 前缀格式、主键 `id` 规范、`created_at/updated_at/deleted_at` 采用 bigint Unix时间戳、以及 `is_del` 软删除逻辑等。)*

## 5. 题库生成方案 (核心痛点解决方案)

由于系统之前依靠正则提取产生的选项含有冗余 Markdown 标记（如 `<font>` 标签），导致生成的题目“驴唇不对马嘴”。为保证题库的绝对高质量，特制定以下**智能体深度消化生成方案**，供后续对话继续执行：

### 5.1 生成思路 (AI 深度消化模式)
抛弃规则死板的“脚本提取模式”。在后续的对话中，利用 AI 智能体的大语言模型能力，真正“阅读并理解” `knowledge_docs` 表中的前端技术原文，由 AI 亲自扮演面试官进行出题。

### 5.2 实施步骤 (紧凑版高质量题库)
1. **提取核心锚点**：系统先查出 `knowledge_docs` 中的核心 `knowledge_point` 名称。
2. **AI 人工撰写**：AI 将针对每个大类的核心知识点（如 Vue 响应式原理、JS 闭包、CSS BFC 等），亲自撰写 50~100 道**高频面试级别的纯正多选题/单选题**。
3. **精准锚定**：在 AI 撰写考题时，将其 `knowledge_point` 字段严格设定为数据库中真实存在的知识点名称。
4. **脚本灌入**：AI 会生成一个 Node 脚本或 SQL 文件，将这些高质量题目一次性注入到 `questions` 表中。

### 5.3 闭环体验
完成高质量题库灌入后：
- 用户做题时，题目专业且极具代表性。
- 看解析时，由于 `knowledge_point` 完美对应，可以直接呈现该知识点的原文解析。
- 对某个知识点彻底迷茫时，通过“参阅文档”模块搜索该知识点，直接溯源到原始教程，形成完美的学习闭环。

---
> **后续对接指引**：在开启新的对话时，可将此文件发给 AI，并直接下达指令：“阅读需求文档，执行第5章的题库生成方案（精品浓缩版），生成高质量考题入库。”
